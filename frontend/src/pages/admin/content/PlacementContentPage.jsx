import PageSectionManager from "../../../components/admin/PageSectionManager";

export default function PlacementContentPage() {
  return (
    <PageSectionManager
      page="placement"
      heading="Placement Page Content"
      description="Edit the headings shown on the public Placement page. The job categories and listings themselves are managed under Job Categories and Job Postings, not here."
    />
  );
}

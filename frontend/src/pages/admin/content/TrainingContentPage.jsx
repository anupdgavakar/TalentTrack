import PageSectionManager from "../../../components/admin/PageSectionManager";

export default function TrainingContentPage() {
  return (
    <PageSectionManager
      page="training"
      heading="Training Page Content"
      description="Edit the heading and intro text shown above the course search on the public Training page. The courses themselves are managed under Courses, not here."
    />
  );
}
